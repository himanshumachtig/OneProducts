{#
/**
 * @file page--taxonomy--term.html.twig
 * Page wrapper for all taxonomy term pages (e.g. /mens/jackets).
 * Header/footer live here; the taxonomy-term template provides the content.
 * No inner container — taxonomy template uses full-width sections.
 */
#}

<div id="spinner"
     class="show bg-white position-fixed translate-middle w-100 vh-100 top-50 start-50 d-flex align-items-center justify-content-center"
     style="z-index:9999;">
  <div class="spinner-border text-primary" style="width:3rem;height:3rem;" role="status">
    <span class="visually-hidden">{{ 'Loading...'|t }}</span>
  </div>
</div>

<div id="page-wrapper">

  {% include directory ~ '/templates/layout/partials/site-header.html.twig' %}

  <div class="highlighted">{{ page.highlighted }}</div>

  <main id="main-content" role="main">
    {{ page.content }}
  </main>

  {% include directory ~ '/templates/layout/partials/site-footer.html.twig' %}

</div>
