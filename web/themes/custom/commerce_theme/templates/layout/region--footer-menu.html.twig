{#
/**
 * @file region--footer-menu.html.twig
 * Renders the footer_menu region WITHOUT a wrapper div so that the block's
 * col-* children are direct siblings of the newsletter column inside row g-5.
 */
#}
{{ content }}
